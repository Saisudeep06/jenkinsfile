pipeline {
    agent any

    stages {
        stage('code and build') {
            steps {
               git 'https://github.com/ramyachetty/web-app.git'
               sh 'mvn clean package'
            }
        }
        stage('hello') {
            steps {
                echo 'hai'
            }
        }
    }
}
